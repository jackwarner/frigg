service: ${env:PIPELINE_SERVICE_NAME}

provider:
  name: aws
  stage: local
  runtime: nodejs6.10
  stackTags:
   repo: ${env:REPO_NAME}
   frigg: pipeline

functions:
  hello:
    handler: handler.hello
